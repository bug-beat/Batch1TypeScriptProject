/**
 * class -> where we store data/variables/properties and methods/actions
 *      variables -> they have data
 *      method -> do some action, reusable action. When you call a method,
 * everything inside the curly brackets will be executed.
 *          name -> defining what method does
 *          arguments -> Dynamic data.
 *          return -> return data/variable/property/etc.
 *              examples for return: void -> nothing to return
 *                                   boolean
 *                                   string
 *                                   etc.   
 *      constructor -> it is a special method for creating a copy of the class
 */